name: Firebase_Interaction_Sample
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  effective_dart: ^1.3.0
  firebase_auth: ^0.20.0+1
  firebase_core: ^0.7.0
  flutter_bloc: ^6.1.3
  flutter_multi_formatter: ^1.3.6
  logger: ^0.9.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: null
  freezed: ^0.12.7

flutter:
flutter_intl:
  enabled: true
  uses-material-design: true
